{
  "scripts": {
    "setup": "bash setup.sh",
    "start": "uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "python run_tests.py",
    "docker-build": "docker build -t travel-social .",
    "docker-run": "docker run -p 7860:7860 -v $(pwd)/uploads:/code/uploads travel-social"
  }
} 